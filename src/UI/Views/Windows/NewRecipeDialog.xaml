<Window x:Class="SunEyeVision.UI.Views.Windows.NewRecipeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="新建配方"
        Width="400"
        SizeToContent="Height"
        MaxHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 配方名称 -->
        <TextBlock Grid.Row="0" Text="配方名称:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1"
                 Name="RecipeNameTextBox"
                 Text="{Binding RecipeName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15"/>

        <!-- 描述 -->
        <TextBlock Grid.Row="2" Text="描述:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3"
                 Name="DescriptionTextBox"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Height="60"
                 Margin="0,0,0,15"/>

        <!-- 按钮 -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="取消"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button Content="确定"
                    Name="OkButton"
                    Width="80"
                    Height="30"
                    Click="OkButton_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
