<Window x:Class="SunEyeVision.UI.Views.Windows.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SunEyeVision.UI"
        xmlns:vm="clr-namespace:SunEyeVision.UI.ViewModels"
        mc:Ignorable="d"
        Title="工具调试" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/AppResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="12,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔧" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding ToolName}" FontSize="16" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center"/>
                <TextBlock Text=" - 调试窗口" FontSize="14" Foreground="#666666" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧图像区域 -->
            <Border Grid.Column="0" Background="#333333" BorderBrush="#666666" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 图像标签 -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="8,8,8,4" Background="#333333">
                        <Button Name="btnOriginal" Click="btnOriginal_Click" Background="#FFFFFF" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10,6" FontSize="11">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📷" FontSize="10" VerticalAlignment="Center"/>
                                <TextBlock Text="原图" Margin="4,0,0,0" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Name="btnProcessed" Click="btnProcessed_Click" Background="Transparent" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10,6" FontSize="11" Margin="4,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔧" FontSize="10" VerticalAlignment="Center"/>
                                <TextBlock Text="处理后" Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Name="btnResult" Click="btnResult_Click" Background="Transparent" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10,6" FontSize="11" Margin="4,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" FontSize="10" VerticalAlignment="Center"/>
                                <TextBlock Text="检测结果" Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- 图像显示 -->
                    <Border Grid.Row="1" Background="#444444" Margin="4" CornerRadius="2">
                        <Grid>
                            <Image Name="imgDisplay" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                            <TextBlock Name="txtNoImage" Text="暂无图像" Foreground="#888888" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- 缩放工具 -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,8,8" Background="#333333">
                        <Button Click="btnZoomIn_Click" Background="White" BorderBrush="#CCCCCC" Padding="6,4" Margin="1" FontSize="10">+</Button>
                        <Button Click="btnZoomOut_Click" Background="White" BorderBrush="#CCCCCC" Padding="6,4" Margin="1" FontSize="10">-</Button>
                        <Button Click="btnFit_Click" Background="White" BorderBrush="#CCCCCC" Padding="6,4" Margin="1" FontSize="10">📐</Button>
                        <Button Click="btn1to1_Click" Background="White" BorderBrush="#CCCCCC" Padding="6,4" Margin="1" FontSize="10">1:1</Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 右侧参数区域 -->
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- 工具信息 -->
                    <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Margin="0,0,0,8">
                        <StackPanel>
                            <Border Background="#F0F8FF" Padding="8" BorderBrush="#0066CC" BorderThickness="0,0,0,2">
                                <TextBlock Text="📖 工具信息" FontWeight="Bold" FontSize="12" Foreground="#0066CC"/>
                            </Border>
                            <StackPanel Margin="8">
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="名称:" VerticalAlignment="Center" FontSize="11" Foreground="#666666"/>
                                    <TextBlock Grid.Column="1" Text="{Binding ToolName}" VerticalAlignment="Center" FontSize="11" Foreground="#333333" FontWeight="SemiBold"/>
                                </Grid>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="类型:" VerticalAlignment="Center" FontSize="11" Foreground="#666666"/>
                                    <TextBlock Grid.Column="1" Text="{Binding ToolType}" VerticalAlignment="Center" FontSize="11" Foreground="#333333"/>
                                </Grid>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="状态:" VerticalAlignment="Center" FontSize="11" Foreground="#666666"/>
                                    <TextBlock Grid.Column="1" Text="{Binding ToolStatus}" VerticalAlignment="Center" FontSize="11" Foreground="#00CC99" FontWeight="SemiBold"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 自定义控件容器 - 支持两种模式 -->
                    <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Margin="0,0,0,8">
                        <StackPanel>
                            <Border Background="#FFF8F0" Padding="8" BorderBrush="#FF9900" BorderThickness="0,0,0,2">
                                <TextBlock Text="🔧 参数配置" FontWeight="Bold" FontSize="12" Foreground="#FF9900"/>
                            </Border>
                            <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                <StackPanel Name="customControlContainer" Margin="8">
                                    <!-- 这里会动态填充自定义控件或自动生成的参数控件 -->
                                </StackPanel>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- 执行控制 -->
                    <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
                        <StackPanel>
                            <Border Background="#F0FFF0" Padding="8" BorderBrush="#00CC99" BorderThickness="0,0,0,2">
                                <TextBlock Text="▶️ 执行控制" FontWeight="Bold" FontSize="12" Foreground="#00CC99"/>
                            </Border>
                            <StackPanel Margin="8">
                            <Button Click="btnRun_Click" Content="运行工具" Padding="12,8" FontSize="12" FontWeight="Bold" Background="#00CC99" Foreground="White" BorderBrush="#00AA77" BorderThickness="1" Cursor="Hand" Margin="0,4">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button Click="btnReset_Click" Content="重置参数" Padding="12,8" FontSize="12" Background="#F0F0F0" Foreground="#666666" BorderBrush="#CCCCCC" BorderThickness="1" Cursor="Hand" Margin="0,4">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- 底部状态栏 -->
        <Border Grid.Row="2" Background="{StaticResource HeaderBrush}" Padding="8,6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" FontSize="11" Foreground="#333333" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="执行时间: " FontSize="11" Foreground="#666666" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ExecutionTime}" FontSize="11" Foreground="#0066CC" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <TextBlock Text="FPS: " FontSize="11" Foreground="#666666" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding FPS}" FontSize="11" Foreground="#00CC99" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
