<UserControl x:Class="SunEyeVision.UI.Views.Controls.Panels.ImageDisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">

    <Border Background="#333333" BorderBrush="#333333" BorderThickness="0">
        <Grid>
            <!-- 图像显示区域 -->
            <Border Background="#000000" Margin="10" BorderBrush="#444444" BorderThickness="1">
                <Image x:Name="MainImage"
                       Source="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                    <Setter Property="Source" Value="{x:Null}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </Border>

            <!-- 提示文本（当没有图像时显示） -->
            <TextBlock x:Name="PlaceholderText"
                       Text="图像显示区域"
                       Foreground="#888888"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- 标签栏 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0" Background="#333333">
                <Button Content="📷 原图"
                        Padding="8,4" Height="28"
                        Background="#FFFFFF"
                        BorderBrush="#DDDDDD"
                        BorderThickness="1"
                        FontSize="11"
                        Command="{Binding SelectOriginalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                <Button Content="🔧 处理后"
                        Padding="8,4" Height="28"
                        Background="Transparent"
                        BorderBrush="#DDDDDD"
                        BorderThickness="1"
                        FontSize="11"
                        Margin="2,0,0,0"
                        Command="{Binding SelectProcessedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                <Button Content="📊 检测结果"
                        Padding="8,4" Height="28"
                        Background="Transparent"
                        BorderBrush="#DDDDDD"
                        BorderThickness="1"
                        FontSize="11"
                        Margin="2,0,0,0"
                        Command="{Binding SelectResultCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </StackPanel>

            <!-- 缩放工具 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
                <Button Content="+"
                        Background="White"
                        BorderBrush="#CCCCCC"
                        Padding="6,4"
                        Margin="1"
                        FontSize="10"
                        Command="{Binding ZoomInCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                <Button Content="-"
                        Background="White"
                        BorderBrush="#CCCCCC"
                        Padding="6,4"
                        Margin="1"
                        FontSize="10"
                        Command="{Binding ZoomOutCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                <Button Content="📐"
                        Background="White"
                        BorderBrush="#CCCCCC"
                        Padding="6,4"
                        Margin="1"
                        FontSize="10"
                        Command="{Binding FitToWindowCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                <Button Content="💾"
                        Background="White"
                        BorderBrush="#CCCCCC"
                        Padding="6,4"
                        Margin="1"
                        FontSize="10"
                        Command="{Binding SaveImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
