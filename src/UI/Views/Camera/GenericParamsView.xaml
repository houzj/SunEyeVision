<UserControl x:Class="SunEyeVision.UI.Views.Camera.GenericParamsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="350">
    
    <StackPanel Margin="8">
        
        <!-- 曝光参数 -->
        <GroupBox Header="曝光参数" Margin="0,0,0,16">
            <StackPanel Margin="8">
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="曝光模式:" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Grid.Column="1" 
                              SelectedItem="{Binding ExposureMode}">
                        <ComboBoxItem Content="自动"/>
                        <ComboBoxItem Content="手动"/>
                        <ComboBoxItem Content="光圈优先"/>
                        <ComboBoxItem Content="快门优先"/>
                    </ComboBox>
                </Grid>
                
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="曝光时间:" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Grid.Column="1" Text="{Binding ExposureTime}"/>
                </Grid>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="增益:" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Grid.Column="1" Text="{Binding Gain}"/>
                </Grid>
            </StackPanel>
        </GroupBox>
        
        <!-- 智能功能 -->
        <GroupBox Header="智能功能" Margin="0,0,0,16">
            <StackPanel Margin="8">
                <CheckBox Content="启用移动侦测" IsChecked="{Binding EnableMotionDetection}" Margin="0,0,0,8"/>
                <CheckBox Content="启用越界检测" IsChecked="{Binding EnableBorderDetection}" Margin="0,0,0,8"/>
                <CheckBox Content="启用区域入侵" IsChecked="{Binding EnableRegionIntrusion}"/>
            </StackPanel>
        </GroupBox>
        
        <!-- 图像增强 -->
        <GroupBox Header="图像增强" Margin="0,0,0,16">
            <StackPanel Margin="8">
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="宽动态:" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Grid.Column="1" 
                              SelectedItem="{Binding WideDynamic}">
                        <ComboBoxItem Content="启用"/>
                        <ComboBoxItem Content="禁用"/>
                    </ComboBox>
                </Grid>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="宽动态等级:" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Grid.Column="1" Text="{Binding WideDynamicLevel}"/>
                </Grid>
            </StackPanel>
        </GroupBox>
        
        <!-- 编码参数 -->
        <GroupBox Header="编码参数">
            <StackPanel Margin="8">
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="压缩类型:" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Grid.Column="1" 
                              SelectedItem="{Binding CompressionType}">
                        <ComboBoxItem Content="H.264"/>
                        <ComboBoxItem Content="H.265"/>
                        <ComboBoxItem Content="MJPEG"/>
                    </ComboBox>
                </Grid>
                
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="码率:" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Grid.Column="1" Text="{Binding Bitrate}"/>
                </Grid>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="I帧间隔:" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Grid.Column="1" Text="{Binding IframeInterval}"/>
                </Grid>
            </StackPanel>
        </GroupBox>
        
    </StackPanel>
</UserControl>
