<UserControl x:Class="SunEyeVision.Plugin.SDK.UI.Controls.Region.Views.DrawingParameterPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SunEyeVision.Plugin.SDK.UI.Controls.Region.Views"
             xmlns:models="clr-namespace:SunEyeVision.Plugin.SDK.UI.Controls.Region.Models"
             xmlns:ctrl="clr-namespace:SunEyeVision.Plugin.SDK.UI.Controls">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:ShapeTypeToVisibilityConverter x:Key="ShapeTypeToVisibilityConverter"/>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        
        <!-- 标签样式 -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,2,4,2"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <!-- 分组标题样式 -->
        <Style x:Key="GroupHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Margin" Value="8,8,8,4"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="8">
                <!-- 提示信息 -->
                <TextBlock Text="绘制参数"
                           Style="{StaticResource GroupHeaderStyle}"/>
                <TextBlock Text="选择形状工具后，在此处可手动输入参数"
                           FontSize="11"
                           Foreground="#666"
                           Margin="8,0,8,8"
                           TextWrapping="Wrap"/>
                
                <!-- 通用位置参数（所有形状显示） -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,1" Padding="0,4" Margin="0,0,0,8"
                        Visibility="{Binding DrawingShapeType, Converter={StaticResource ShapeTypeToVisibilityConverter}, ConverterParameter='Rectangle,Circle,RotatedRectangle', FallbackValue=Collapsed}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="中心X:" Style="{StaticResource LabelStyle}"/>
                        <ctrl:NumericUpDown Grid.Column="1"
                                            Value="{Binding PreviewShape.CenterX, UpdateSourceTrigger=PropertyChanged, FallbackValue=0}"
                                            Minimum="0" Maximum="9999"
                                            SmallChange="1" LargeChange="10"
                                            DecimalPlaces="3"
                                            Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                        <TextBlock Grid.Column="2" Text="Y:" Style="{StaticResource LabelStyle}" Margin="8,2,4,2"/>
                        <ctrl:NumericUpDown Grid.Column="3"
                                            Value="{Binding PreviewShape.CenterY, UpdateSourceTrigger=PropertyChanged, FallbackValue=0}"
                                            Minimum="0" Maximum="9999"
                                            SmallChange="1" LargeChange="10"
                                            DecimalPlaces="3"
                                            Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                    </Grid>
                </Border>
                
                <!-- 矩形参数 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,1" Padding="0,4" Margin="0,0,0,8"
                        Visibility="{Binding DrawingShapeType, Converter={StaticResource ShapeTypeToVisibilityConverter}, ConverterParameter='Rectangle', FallbackValue=Collapsed}">
                    <StackPanel>
                        <TextBlock Text="矩形尺寸 - 矩形工具"
                                   FontSize="12"
                                   Foreground="#666"
                                   Margin="8,4"/>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="宽度:" Style="{StaticResource LabelStyle}"/>
                            <ctrl:NumericUpDown Grid.Column="1"
                                                Value="{Binding PreviewShape.Width, UpdateSourceTrigger=PropertyChanged, FallbackValue=100}"
                                                Minimum="1" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                            <TextBlock Grid.Column="2" Text="高度:" Style="{StaticResource LabelStyle}" Margin="8,2,4,2"/>
                            <ctrl:NumericUpDown Grid.Column="3"
                                                Value="{Binding PreviewShape.Height, UpdateSourceTrigger=PropertyChanged, FallbackValue=100}"
                                                Minimum="1" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- 圆形参数 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,1" Padding="0,4" Margin="0,0,0,8"
                        Visibility="{Binding DrawingShapeType, Converter={StaticResource ShapeTypeToVisibilityConverter}, ConverterParameter='Circle', FallbackValue=Collapsed}">
                    <StackPanel>
                        <TextBlock Text="圆形尺寸 - 圆形工具"
                                   FontSize="12"
                                   Foreground="#666"
                                   Margin="8,4"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="半径:" Style="{StaticResource LabelStyle}"/>
                            <ctrl:NumericUpDown Grid.Column="1"
                                                Value="{Binding PreviewShape.Radius, UpdateSourceTrigger=PropertyChanged, FallbackValue=50}"
                                                Minimum="1" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- 旋转矩形参数 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,1" Padding="0,4" Margin="0,0,0,8"
                        Visibility="{Binding DrawingShapeType, Converter={StaticResource ShapeTypeToVisibilityConverter}, ConverterParameter='RotatedRectangle', FallbackValue=Collapsed}">
                    <StackPanel>
                        <TextBlock Text="旋转矩形尺寸 - 旋转矩形工具"
                                   FontSize="12"
                                   Foreground="#666"
                                   Margin="8,4"/>

                        <!-- 宽高 -->
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="宽度:" Style="{StaticResource LabelStyle}"/>
                            <ctrl:NumericUpDown Grid.Column="1"
                                                Value="{Binding PreviewShape.Width, UpdateSourceTrigger=PropertyChanged, FallbackValue=100}"
                                                Minimum="1" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                            <TextBlock Grid.Column="2" Text="高度:" Style="{StaticResource LabelStyle}" Margin="8,2,4,2"/>
                            <ctrl:NumericUpDown Grid.Column="3"
                                                Value="{Binding PreviewShape.Height, UpdateSourceTrigger=PropertyChanged, FallbackValue=100}"
                                                Minimum="1" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                        </Grid>

                        <!-- 旋转角度 -->
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="角度:" Style="{StaticResource LabelStyle}"/>
                            <Slider Grid.Column="1"
                                    Value="{Binding PreviewShape.Angle, UpdateSourceTrigger=PropertyChanged, FallbackValue=0}"
                                    Minimum="-180" Maximum="180"
                                    TickFrequency="15"
                                    TickPlacement="BottomRight"
                                    Margin="4,2"/>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding PreviewShape.Angle, StringFormat={}{0:F1}°, FallbackValue='0°'}"
                                       VerticalAlignment="Center"
                                       Margin="4,2"
                                       Width="50"
                                       Foreground="#333"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- 直线参数 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,1" Padding="0,4" Margin="0,0,0,8"
                        Visibility="{Binding DrawingShapeType, Converter={StaticResource ShapeTypeToVisibilityConverter}, ConverterParameter='Line', FallbackValue=Collapsed}">
                    <StackPanel>
                        <TextBlock Text="直线端点 - 直线工具"
                                   FontSize="12"
                                   Foreground="#666"
                                   Margin="8,4"/>

                        <!-- 起点 -->
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="起点:" Style="{StaticResource LabelStyle}"/>
                            <ctrl:NumericUpDown Grid.Column="1"
                                                Value="{Binding PreviewShape.StartX, UpdateSourceTrigger=PropertyChanged, FallbackValue=0}"
                                                Minimum="0" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                            <TextBlock Grid.Column="2" Text="Y:" Style="{StaticResource LabelStyle}" Margin="8,2,4,2"/>
                            <ctrl:NumericUpDown Grid.Column="3"
                                                Value="{Binding PreviewShape.StartY, UpdateSourceTrigger=PropertyChanged, FallbackValue=0}"
                                                Minimum="0" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                        </Grid>

                        <!-- 终点 -->
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="终点:" Style="{StaticResource LabelStyle}"/>
                            <ctrl:NumericUpDown Grid.Column="1"
                                                Value="{Binding PreviewShape.EndX, UpdateSourceTrigger=PropertyChanged, FallbackValue=100}"
                                                Minimum="0" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                            <TextBlock Grid.Column="2" Text="Y:" Style="{StaticResource LabelStyle}" Margin="8,2,4,2"/>
                            <ctrl:NumericUpDown Grid.Column="3"
                                                Value="{Binding PreviewShape.EndY, UpdateSourceTrigger=PropertyChanged, FallbackValue=100}"
                                                Minimum="0" Maximum="9999"
                                                SmallChange="1" LargeChange="10"
                                                DecimalPlaces="3"
                                                Width="100" Height="24" Margin="4,2" HorizontalAlignment="Left"/>
                        </Grid>

                        <!-- 长度信息 -->
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="长度:" Style="{StaticResource LabelStyle}"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding PreviewLength, StringFormat=F1}"
                                       VerticalAlignment="Center"
                                       Margin="4,2"
                                       Foreground="#333"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- 未选择形状的提示 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,1" Padding="16,24" Margin="0,0,0,8"
                        Visibility="{Binding IsAnyShapeSelected, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                        Background="#FAFAFA">
                    <TextBlock Text="请先选择形状工具"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#999"
                               FontSize="14"
                               FontStyle="Italic"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
