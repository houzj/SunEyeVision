<UserControl x:Class="SunEyeVision.Plugin.SDK.UI.Controls.Region.Views.ParameterPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:SunEyeVision.Plugin.SDK.UI.Controls.Region.Converters"
             x:Name="Root">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <Border Grid.Row="0" 
                Background="#F0F0F0" 
                Padding="8,4"
                BorderBrush="#D0D0D0"
                BorderThickness="0,0,0,1">
            <TextBlock Text="参数绑定" 
                       FontWeight="Bold"
                       FontSize="12"/>
        </Border>

        <!-- 参数列表 -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Parameters}" Margin="4">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0"
                                BorderThickness="0,0,0,1"
                                Padding="8,6"
                                Margin="0,0,0,2">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 参数名称和绑定按钮 -->
                                <Grid Grid.Row="0" Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 参数名称 -->
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding DisplayName}"
                                               VerticalAlignment="Center"
                                               FontSize="11"
                                               Foreground="#333333"
                                               Width="60"/>

                                    <!-- 绑定路径显示 -->
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DisplayPath}"
                                               VerticalAlignment="Center"
                                               FontSize="10"
                                               Foreground="#666666"
                                               Margin="8,0,4,0"
                                               TextTrimming="CharacterEllipsis"
                                               TextWrapping="NoWrap"/>

                                    <!-- 绑定/清除按钮 -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <!-- 绑定按钮 -->
                                        <Button Content="🔗"
                                                Tag="{Binding}"
                                                Click="BindButton_Click"
                                                ToolTip="绑定数据源"
                                                Padding="4,2"
                                                Margin="2,0"
                                                FontSize="12"
                                                Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="BorderBrush" Value="#D0D0D0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsBound}" Value="True">
                                                            <Setter Property="Background" Value="#E3F2FD"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <!-- 清除按钮 -->
                                        <Button Content="✕"
                                                Tag="{Binding}"
                                                Click="ClearButton_Click"
                                                ToolTip="清除绑定"
                                                Padding="4,2"
                                                Margin="2,0"
                                                FontSize="10"
                                                Cursor="Hand"
                                                Visibility="{Binding IsBound, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="BorderBrush" Value="#D0D0D0"/>
                                                    <Setter Property="Foreground" Value="#F44336"/>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Grid>

                                <!-- 当前值显示 -->
                                <Grid Grid.Row="1" 
                                      Visibility="{Binding IsBound, Converter={StaticResource BoolToVisibilityConverter}}"
                                      Margin="68,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="值:"
                                               FontSize="9"
                                               Foreground="#888888"
                                               VerticalAlignment="Center"
                                               Margin="0,0,4,0"/>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding CurrentValue}"
                                               FontSize="9"
                                               Foreground="#4CAF50"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
