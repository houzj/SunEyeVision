<Window x:Class="SunEyeVision.Plugin.Infrastructure.UI.Windows.EnhancedToolDebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SunEyeVision.Plugin.Infrastructure.Base"
        xmlns:tools="clr-namespace:SunEyeVision.Plugin.Infrastructure.Base"
        xmlns:converters="clr-namespace:SunEyeVision.Plugin.Infrastructure.UI.Converters"
        xmlns:parameters="clr-namespace:SunEyeVision.Plugin.Infrastructure"
        mc:Ignorable="d"
        Title="{Binding ToolName}" Height="800" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SunEyeVision.UI;component/Resources/AppResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 转换器 -->
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:InvertBoolConverter x:Key="InvertBoolConverter"/>
            <converters:ProgressToStringConverter x:Key="ProgressToStringConverter"/>
            <converters:MultiBooleanAndConverter x:Key="MultiBooleanAndConverter"/>

            <!-- 样式 -->
            <Style TargetType="Button" x:Key="PrimaryButton">
                <Setter Property="Background" Value="#2196F3"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#1976D2"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#1976D2"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="Button" x:Key="SecondaryButton">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Foreground" Value="#333333"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#F0F0F0"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <Style TargetType="Border" x:Key="CardBorder">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="Margin" Value="8"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="16,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🔧" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding ToolName}" FontSize="18" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="状态: " FontSize="12" Foreground="#1565C0"/>
                            <TextBlock Text="{Binding ToolStatus}" FontSize="12" FontWeight="SemiBold" Foreground="#1565C0" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 错误提示区 -->
        <Border Grid.Row="1" Background="#FFEBEE" Padding="12,8" BorderBrush="#FFCDD2" BorderThickness="0,0,0,1"
                Visibility="{Binding ValidationError, Converter={StaticResource StringToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠️" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding ValidationError}" FontSize="13" Foreground="#D32F2F" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 参数配置区 -->
                <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="12,12,12,8">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="⚙️" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="参数配置" FontSize="16" FontWeight="SemiBold" Foreground="#333333" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- 动态参数控件 -->
                        <ItemsControl ItemsSource="{Binding ParameterItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="180"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 参数标签 -->
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="13" Foreground="#333333"/>
                                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#666666" TextWrapping="Wrap"
                                                       Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        </StackPanel>

                                        <!-- 参数控件 -->
                                        <ContentPresenter Grid.Column="1" Content="{Binding Control}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- 自定义内容区域（子类可覆盖） -->
                        <ContentControl x:Name="CustomContentArea" Margin="0,8,0,0"
                                        Visibility="{Binding HasCustomContent, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                </Border>

                <!-- 执行结果区 -->
                <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="12,8,12,12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="📊" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="执行结果" FontSize="16" FontWeight="SemiBold" Foreground="#333333" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- 进度条 -->
                        <Grid Margin="0,0,0,12" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ProgressBar Grid.Row="0" Height="8" Value="{Binding Progress}" Maximum="100"
                                         Background="#E0E0E0" Foreground="#2196F3" BorderThickness="0"/>

                            <TextBlock Grid.Row="1" Text="{Binding ProgressMessage}" FontSize="12" Foreground="#666666"
                                       Margin="0,4,0,0"/>
                        </Grid>

                        <!-- 执行结果内容（子类填充） -->
                        <ContentControl x:Name="ResultContentArea" MinHeight="200"
                                        Content="{Binding ResultContent}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- 操作按钮区 -->
        <Border Grid.Row="3" Background="White" Padding="16,12" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Style="{StaticResource PrimaryButton}" Command="{Binding RunCommand}" Margin="4,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="▶" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="运行" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}" Margin="4,0"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏹" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="取消" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SecondaryButton}" Command="{Binding ValidateCommand}" Margin="4,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✓" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="验证参数" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SecondaryButton}" Command="{Binding ResetCommand}" Margin="4,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="↺" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="重置" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Separator Width="1" Margin="16,4"/>

                <Button Style="{StaticResource SecondaryButton}" Command="{Binding SaveCommand}" Margin="4,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="保存" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SecondaryButton}" Command="{Binding LoadCommand}" Margin="4,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📂" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="加载" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Separator Width="1" Margin="16,4"/>

                <Button Style="{StaticResource SecondaryButton}" Command="{Binding CreateSnapshotCommand}" Margin="4,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📷" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="快照" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- 底部状态栏 -->
        <Border Grid.Row="4" Background="{StaticResource HeaderBrush}" Padding="12,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" FontSize="12" Foreground="#333333" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Border Background="#F5F5F5" CornerRadius="4" Padding="6,3" Margin="0,0,8,0">
                        <TextBlock FontSize="11" Foreground="#666666">
                            <Run Text="执行时间: "/>
                            <Run Text="{Binding ExecutionTime}" Foreground="#2196F3" FontWeight="SemiBold"/>
                        </TextBlock>
                    </Border>

                    <Border Background="#F5F5F5" CornerRadius="4" Padding="6,3">
                        <TextBlock FontSize="11" Foreground="#666666">
                            <Run Text="FPS: "/>
                            <Run Text="{Binding FPS}" Foreground="#4CAF50" FontWeight="SemiBold"/>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
