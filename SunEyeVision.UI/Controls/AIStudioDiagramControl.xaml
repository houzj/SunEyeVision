<UserControl x:Class="SunEyeVision.UI.Controls.AIStudioDiagramControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             x:Name="UserControlRoot">
    <Grid Background="#F5F5F5">
        <!-- 节点和连接的画布 -->
        <Canvas x:Name="DiagramCanvas"
                Background="Transparent"
                ClipToBounds="True"
                AllowDrop="True"
                MouseMove="DiagramCanvas_MouseMove"
                DragEnter="DiagramCanvas_DragEnter"
                DragOver="DiagramCanvas_DragOver"
                DragLeave="DiagramCanvas_DragLeave"
                Drop="DiagramCanvas_Drop">
            <!-- 连接线将在此处渲染 -->
            <Canvas x:Name="ConnectionsLayer" Panel.ZIndex="1"/>
            <!-- 节点将在此处渲染 -->
            <Canvas x:Name="NodesLayer" Panel.ZIndex="2"/>
        </Canvas>

        <!-- 默认提示文本 - 使用 ZIndex 确保它在画布之上 -->
        <TextBlock x:Name="EmptyStateText"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Foreground="#999999"
                   Text="AIStudio Diagram View (Simplified Implementation)"
                   Panel.ZIndex="1000"
                   IsHitTestVisible="False">
        </TextBlock>
    </Grid>
</UserControl>