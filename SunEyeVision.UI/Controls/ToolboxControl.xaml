<UserControl x:Class="SunEyeVision.UI.Controls.ToolboxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SunEyeVision.UI.Controls"
             xmlns:converters="clr-namespace:SunEyeVision.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="250">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <DockPanel>
        <!-- 搜索框和折叠/展开按钮 -->
        <Border DockPanel.Dock="Top" Padding="8,8,8,4" Background="#FFFFFF" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="1"
                         Padding="8,6" FontSize="12" Height="32"
                         VerticalContentAlignment="Center">
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <Grid>
                                    <TextBlock Text="🔍" Margin="8,0,4,0" FontSize="14" VerticalAlignment="Center" Foreground="#999999"/>
                                    <ScrollViewer x:Name="PART_ContentHost" Margin="24,0,8,0" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
                <!-- 折叠/展开按钮 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                    <Button Content="📂 全部展开" Command="{Binding ExpandAllCommand}"
                            Padding="8,4" Margin="2,0" FontSize="11" Background="#F0F8FF"
                            BorderBrush="#0066CC" BorderThickness="1"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="3" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="📁 全部折叠" Command="{Binding CollapseAllCommand}"
                            Padding="8,4" Margin="2,0" FontSize="11" Background="#FFF8F0"
                            BorderBrush="#FF9900" BorderThickness="1"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="3" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 工具箱列表 -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Categories}" Margin="4">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,4" CornerRadius="4">
                            <DockPanel>
                                <!-- 分类标题栏（可折叠） -->
                                <Border DockPanel.Dock="Top" Background="#F0F0F0" Padding="8,10,8,10"
                                        Cursor="Hand"
                                        MouseLeftButtonUp="CategoryHeader_MouseLeftButtonUp"
                                        Tag="{Binding}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="#333333"
                                                   VerticalAlignment="Center"/>

                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <TextBlock Text="{Binding ToolCount}"
                                                       FontSize="11"
                                                       Foreground="#666666"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                            <TextBlock Text="▼" FontSize="10"
                                                       Foreground="#999999"
                                                       VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="RenderTransform">
                                                            <Setter.Value>
                                                                <RotateTransform Angle="0"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                                                                <Setter Property="RenderTransform">
                                                                    <Setter.Value>
                                                                        <RotateTransform Angle="-90"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- 工具列表（可折叠） -->
                                <Border Padding="8" Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ItemsControl ItemsSource="{Binding FilteredToolsForCategory}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Padding="8" Margin="3" Background="#F5F5F5" BorderBrush="#D0D0D0" BorderThickness="1"
                                                        CornerRadius="3"
                                                        Cursor="Hand"
                                                        Width="80" Height="70"
                                                        ToolTipService.ToolTip="{Binding Description}"
                                                        ToolTipService.ShowDuration="5000"
                                                        PreviewMouseLeftButtonDown="ToolItem_PreviewMouseLeftButtonDown"
                                                        Tag="{Binding}">
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Icon}" FontSize="20" HorizontalAlignment="Center" Margin="0,2,0,4"/>
                                                        <TextBlock Text="{Binding Name}" FontSize="9" Foreground="#333333" HorizontalAlignment="Center" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </Border>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
