<Window x:Class="SunEyeVision.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SunEyeVision.UI"
        xmlns:vm="clr-namespace:SunEyeVision.UI.ViewModels"
        xmlns:converters="clr-namespace:SunEyeVision.UI.Converters"
        xmlns:controls="clr-namespace:SunEyeVision.UI.Controls"
        mc:Ignorable="d"
        Title="太阳眼视觉 - 机器视觉平台" Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#FFFFFF"
        Loaded="MainWindow_Loaded">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/AppResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:RunModeConverter x:Key="RunModeConverter"/>
            <converters:BoolToRunningTextConverter x:Key="BoolToRunningTextConverter"/>
            <converters:BoolToContinuousTextConverter x:Key="BoolToContinuousTextConverter"/>
            <converters:BoolToRunningBackgroundConverter x:Key="BoolToRunningBackgroundConverter"/>
            <converters:BoolToRunningBorderConverter x:Key="BoolToRunningBorderConverter"/>
            <converters:RunModeButtonConverter x:Key="RunModeButtonConverter"/>
            <converters:BoolToSelectedBackgroundConverter x:Key="BoolToSelectedBackgroundConverter"/>
            <converters:BoolToSelectedBorderConverter x:Key="BoolToSelectedBorderConverter"/>
            <converters:BoolToSelectedBorderThicknessConverter x:Key="BoolToSelectedBorderThicknessConverter"/>
            <converters:BoolToActiveConverter x:Key="BoolToActiveConverter"/>
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel>
        <!-- ========== 顶部菜单栏 (28px) ========== -->
        <Menu DockPanel.Dock="Top" Height="28" Background="{StaticResource HeaderBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <MenuItem Header="文件(_F)">
                <MenuItem Header="新建方案(_N)" Command="{Binding NewWorkflowCommand}" InputGestureText="Ctrl+N"/>
                <MenuItem Header="打开方案(_O)" Command="{Binding OpenWorkflowCommand}" InputGestureText="Ctrl+O"/>
                <MenuItem Header="保存方案(_S)" Command="{Binding SaveWorkflowCommand}" InputGestureText="Ctrl+S"/>
                <MenuItem Header="另存为(_A)" Command="{Binding SaveAsWorkflowCommand}" InputGestureText="Ctrl+Shift+S"/>
                <Separator/>
                <MenuItem Header="导入(_I)" InputGestureText="Ctrl+I"/>
                <MenuItem Header="导出(_E)" InputGestureText="Ctrl+E"/>
                <Separator/>
                <MenuItem Header="最近文件"/>
                <Separator/>
                <MenuItem Header="退出(_X)" InputGestureText="Alt+F4"/>
            </MenuItem>
            <MenuItem Header="编辑(_E)">
                <MenuItem Header="撤销(_U)" InputGestureText="Ctrl+Z"/>
                <MenuItem Header="重做(_R)" InputGestureText="Ctrl+Y"/>
                <Separator/>
                <MenuItem Header="复制(_C)" InputGestureText="Ctrl+C"/>
                <MenuItem Header="粘贴(_V)" InputGestureText="Ctrl+V"/>
                <MenuItem Header="删除(_D)" InputGestureText="Del"/>
                <Separator/>
                <MenuItem Header="全选(_A)" InputGestureText="Ctrl+A"/>
                <MenuItem Header="查找(_F)" InputGestureText="Ctrl+F"/>
                <MenuItem Header="替换(_H)" InputGestureText="Ctrl+H"/>
            </MenuItem>
            <MenuItem Header="视图(_V)">
                <MenuItem Header="方案树(_S)"/>
                <MenuItem Header="工具箱(_T)"/>
                <MenuItem Header="设备管理(_D)"/>
                <MenuItem Header="属性面板(_P)"/>
                <MenuItem Header="日志窗口(_L)"/>
                <MenuItem Header="结果预览(_R)"/>
                <MenuItem Header="系统信息(_I)"/>
                <Separator/>
                <MenuItem Header="全屏(_F)" InputGestureText="F11"/>
                <MenuItem Header="重置布局(_R)" Command="{Binding ResetLayoutCommand}"/>
            </MenuItem>
            <MenuItem Header="运行(_R)">
                <MenuItem Header="运行(_R)" Command="{Binding RunWorkflowCommand}" InputGestureText="F5"/>
                <MenuItem Header="停止(_S)" Command="{Binding StopWorkflowCommand}" InputGestureText="Shift+F5"/>
                <MenuItem Header="暂停(_P)" InputGestureText="Ctrl+Break"/>
                <Separator/>
                <MenuItem Header="调试模式(_D)" InputGestureText="Ctrl+Shift+D"/>
                <MenuItem Header="单步执行(_S)" InputGestureText="F10"/>
                <MenuItem Header="断点(_B)" InputGestureText="F9"/>
            </MenuItem>
            <MenuItem Header="工具(_T)">
                <MenuItem Header="相机配置(_C)"/>
                <MenuItem Header="参数管理(_P)"/>
                <MenuItem Header="插件管理(_L)"/>
                <MenuItem Header="系统设置(_S)"/>
                <MenuItem Header="快捷键设置(_K)"/>
            </MenuItem>
            <MenuItem Header="插件(_P)">
                <MenuItem Header="插件市场(_M)"/>
                <MenuItem Header="安装插件(_I)"/>
                <MenuItem Header="启用/禁用(_D)"/>
                <MenuItem Header="插件更新(_U)"/>
            </MenuItem>
            <MenuItem Header="帮助(_H)">
                <MenuItem Header="用户手册(_M)" InputGestureText="F1" Command="{Binding ShowHelpCommand}"/>
                <MenuItem Header="API文档(_D)"/>
                <MenuItem Header="快捷键(_K)" Command="{Binding ShowShortcutsCommand}"/>
                <MenuItem Header="关于(_A)" Command="{Binding ShowAboutCommand}"/>
            </MenuItem>
        </Menu>

        <!-- ========== 工具栏 (40px) ========== -->
        <ToolBar DockPanel.Dock="Top" Height="40" Background="{StaticResource HeaderBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <!-- 文件操作组 -->
            <Button ToolTip="新建 (Ctrl+N)" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}" Command="{Binding NewWorkflowCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📄" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="新建" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button ToolTip="打开 (Ctrl+O)" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}" Command="{Binding OpenWorkflowCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📂" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="打开" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button ToolTip="保存 (Ctrl+S)" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}" Command="{Binding SaveWorkflowCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💾" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="保存" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Separator/>

            <!-- 执行控制组 -->
            <Button ToolTip="运行 (F5)" Padding="8,4" Margin="2" Style="{StaticResource PrimaryButton}" Command="{Binding RunWorkflowCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="▶️" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="运行" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button ToolTip="停止 (Shift+F5)" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}" Command="{Binding StopWorkflowCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⏹️" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="停止" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button ToolTip="暂停" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⏸️" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="暂停" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Separator/>

            <!-- 调试工具组 -->
            <Button ToolTip="单步执行 (F10)" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="调试" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Separator/>

            <!-- 编辑操作组 -->
            <Button ToolTip="撤销 (Ctrl+Z)" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}">
                <TextBlock Text="↩️" FontSize="16" VerticalAlignment="Center"/>
            </Button>
            <Button ToolTip="重做 (Ctrl+Y)" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}">
                <TextBlock Text="↪️" FontSize="16" VerticalAlignment="Center"/>
            </Button>

            <Separator/>

            <!-- 视图控制组 -->
            <Button ToolTip="放大" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}">
                <TextBlock Text="🔎+" FontSize="14" VerticalAlignment="Center"/>
            </Button>
            <Button ToolTip="缩小" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}">
                <TextBlock Text="🔎-" FontSize="14" VerticalAlignment="Center"/>
            </Button>
            <Button ToolTip="适应窗口" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}">
                <TextBlock Text="📐" FontSize="16" VerticalAlignment="Center"/>
            </Button>
            <Button ToolTip="100%" Padding="8,4" Margin="2" Style="{StaticResource SecondaryButton}">
                <TextBlock Text="1:1" FontSize="14" VerticalAlignment="Center" FontWeight="Bold"/>
            </Button>

            <Separator/>

            <TextBlock Text="缩放: 100%" VerticalAlignment="Center" Margin="8,0,4,0" FontSize="12" Foreground="#666666"/>
        </ToolBar>


        <!-- ========== 主内容区域 ========== -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" x:Name="LeftColumn"/>
                <ColumnDefinition Width="5" x:Name="LeftSplitterColumn"/>
                <ColumnDefinition Width="*" x:Name="MiddleColumn"/>
                <ColumnDefinition Width="5" x:Name="RightSplitterColumn"/>
                <ColumnDefinition Width="500" x:Name="RightColumn"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="24"/>
            </Grid.RowDefinitions>

            <!-- ========== 左侧分隔符 (可拖动 + 可折叠) ========== -->
            <GridSplitter Grid.Column="1" Width="5" Background="#E0E0E0" HorizontalAlignment="Stretch"
                          ResizeDirection="Columns" ResizeBehavior="PreviousAndNext"
                          DragDelta="LeftSplitter_DragDelta"
                          MouseDoubleClick="LeftSplitter_MouseDoubleClick"
                          Cursor="SizeWE">
                <GridSplitter.Style>
                    <Style TargetType="GridSplitter">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0066CC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </GridSplitter.Style>
            </GridSplitter>
            
            <!-- 左侧折叠按钮 - 根据折叠状态动态调整位置 -->
            <Button x:Name="LeftCollapseButton" Grid.Column="0" Panel.ZIndex="100"
                    Width="28" Height="28" Background="#FFFFFF" BorderThickness="1"
                    BorderBrush="#CCCCCC"
                    HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-14,0"
                    ToolTip="折叠/展开左侧面板 (双击分隔符也可以)"
                    Cursor="Hand"
                    Opacity="0.9"
                    MouseEnter="LeftCollapseButton_MouseEnter"
                    MouseLeave="LeftCollapseButton_MouseLeave"
                    Click="LeftCollapseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4"
                                            Opacity="{TemplateBinding Opacity}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0066CC"/>
                                <Setter Property="BorderBrush" Value="#0066CC"/>
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <TextBlock x:Name="LeftCollapseButtonText" Text="◀" FontSize="16" FontWeight="Bold"
                           Foreground="#666666" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Button>

            <!-- ========== 左侧工具区 (可调整宽度) ========== -->
            <Border Grid.Column="0" x:Name="LeftPanel" Background="#F8F8F8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <DockPanel>
                    <!-- 搜索框和折叠/展开按钮 -->
                    <Border DockPanel.Dock="Top" Padding="8,8,8,4" Background="#FFFFFF" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <StackPanel>
                            <TextBox Text="{Binding Toolbox.SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="1"
                                     Padding="8,6" FontSize="12" Height="32"
                                     VerticalContentAlignment="Center">
                                <TextBox.Template>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <Grid>
                                                <TextBlock Text="🔍" Margin="8,0,4,0" FontSize="14" VerticalAlignment="Center" Foreground="#999999"/>
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="24,0,8,0" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                            <!-- 折叠/展开按钮 -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                                <Button Content="📂 全部展开" Command="{Binding Toolbox.ExpandAllCommand}"
                                        Padding="8,4" Margin="2,0" FontSize="11" Background="#F0F8FF"
                                        BorderBrush="#0066CC" BorderThickness="1"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="3" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Button Content="📁 全部折叠" Command="{Binding Toolbox.CollapseAllCommand}"
                                        Padding="8,4" Margin="2,0" FontSize="11" Background="#FFF8F0"
                                        BorderBrush="#FF9900" BorderThickness="1"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="3" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 工具箱列表 -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Toolbox.Categories}" Margin="4">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,4" CornerRadius="4">
                                        <DockPanel>
                                            <!-- 分类标题栏（可折叠） -->
                                            <Border DockPanel.Dock="Top" Background="#F0F0F0" Padding="8,10,8,10"
                                                    Cursor="Hand"
                                                    MouseLeftButtonUp="CategoryHeader_MouseLeftButtonUp"
                                                    Tag="{Binding}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>

                                                    <TextBlock Grid.Column="1"
                                                               Text="{Binding Name}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="#333333"
                                                               VerticalAlignment="Center"/>

                                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                        <TextBlock Text="{Binding ToolCount}"
                                                                   FontSize="11"
                                                                   Foreground="#666666"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0,0,8,0"/>
                                                        <TextBlock Text="▼" FontSize="10"
                                                                   Foreground="#999999"
                                                                   VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="RenderTransform">
                                                                        <Setter.Value>
                                                                            <RotateTransform Angle="0"/>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                                                                            <Setter Property="RenderTransform">
                                                                                <Setter.Value>
                                                                                    <RotateTransform Angle="-90"/>
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>

                                            <!-- 工具列表（可折叠） -->
                                            <Border Padding="8" Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <ItemsControl ItemsSource="{Binding FilteredToolsForCategory}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Padding="8" Margin="3" Background="#F5F5F5" BorderBrush="#D0D0D0" BorderThickness="1"
                                                                    CornerRadius="3"
                                                                    Cursor="Hand"
                                                                    Width="80" Height="70"
                                                                    ToolTipService.ToolTip="{Binding Description}"
                                                                    ToolTipService.ShowDuration="5000"
                                                                    PreviewMouseLeftButtonDown="ToolItem_PreviewMouseLeftButtonDown"
                                                                    Tag="{Binding}">
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding Icon}" FontSize="20" HorizontalAlignment="Center" Margin="0,2,0,4"/>
                                                                    <TextBlock Text="{Binding Name}" FontSize="9" Foreground="#333333" HorizontalAlignment="Center" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </Border>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- ========== 中间工作区 ========== -->
            <Border Grid.Column="2" Background="#F5F5F5" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
                <controls:WorkflowCanvasControl
                    x:Name="WorkflowCanvas"
                    Workflows="{Binding Workflows}"
                    CurrentWorkflow="{Binding CurrentWorkflow}"
                    Nodes="{Binding WorkflowNodes}"
                    Connections="{Binding WorkflowConnections}"
                    NodeDoubleClicked="WorkflowCanvas_NodeDoubleClicked"
                    Margin="0"/>
            </Border>



            <!-- ========== 右侧分隔符 (可拖动 + 可折叠) ========== -->
            <GridSplitter Grid.Column="3" Width="5" Background="#E0E0E0" HorizontalAlignment="Stretch"
                          ResizeDirection="Columns" ResizeBehavior="PreviousAndNext"
                          DragDelta="RightSplitter_DragDelta"
                          MouseDoubleClick="RightSplitter_MouseDoubleClick"
                          Cursor="SizeWE">
                <GridSplitter.Style>
                    <Style TargetType="GridSplitter">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0066CC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </GridSplitter.Style>
            </GridSplitter>
            
            <!-- 右侧折叠按钮 - 根据折叠状态动态调整位置 -->
            <Button x:Name="RightCollapseButton" Grid.Column="4" Panel.ZIndex="100"
                    Width="28" Height="28" Background="#FFFFFF" BorderThickness="1"
                    BorderBrush="#CCCCCC"
                    HorizontalAlignment="Left" VerticalAlignment="Center" Margin="-14,0,0,0"
                    ToolTip="折叠/展开右侧面板 (双击分隔符也可以)"
                    Cursor="Hand"
                    Opacity="0.9"
                    MouseEnter="RightCollapseButton_MouseEnter"
                    MouseLeave="RightCollapseButton_MouseLeave"
                    Click="RightCollapseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4"
                                            Opacity="{TemplateBinding Opacity}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0066CC"/>
                                <Setter Property="BorderBrush" Value="#0066CC"/>
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <TextBlock x:Name="RightCollapseButtonText" Text="▶" FontSize="16" FontWeight="Bold"
                           Foreground="#666666" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Button>

            <!-- ========== 右侧面板 (可调整宽度) ========== -->
            <Border Grid.Column="4" x:Name="RightPanel" Grid.Row="0" Background="#FFFFFF" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="500"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 图像显示区域 (500px) -->
                    <Border Grid.Row="0" Background="#333333" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <Grid>
                            <TextBlock Text="图像显示区域" Foreground="#888888" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <!-- 标签栏 -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,8,0,0" Background="#333333">
                                <Button Background="#FFFFFF" BorderBrush="#DDDDDD" BorderThickness="1" Padding="8,4" Height="28" HorizontalContentAlignment="Center" FontSize="11">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📷" FontSize="10" VerticalAlignment="Center"/>
                                        <TextBlock Text="原图" Margin="4,0,0,0" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Background="Transparent" BorderBrush="#DDDDDD" BorderThickness="1" Padding="8,4" Height="28" HorizontalContentAlignment="Center" FontSize="11" Margin="2,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔄" FontSize="10" VerticalAlignment="Center"/>
                                        <TextBlock Text="处理后" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Background="Transparent" BorderBrush="#DDDDDD" BorderThickness="1" Padding="8,4" Height="28" HorizontalContentAlignment="Center" FontSize="11" Margin="2,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📊" FontSize="10" VerticalAlignment="Center"/>
                                        <TextBlock Text="检测结果" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                            <!-- 缩放工具 -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="8">
                                <Button Background="White" BorderBrush="#CCCCCC" Padding="6,4" Margin="1" FontSize="10">+</Button>
                                <Button Background="White" BorderBrush="#CCCCCC" Padding="6,4" Margin="1" FontSize="10">-</Button>
                                <Button Background="White" BorderBrush="#CCCCCC" Padding="6,4" Margin="1" FontSize="10">📐</Button>
                                <Button Background="White" BorderBrush="#CCCCCC" Padding="6,4" Margin="1" FontSize="10">💾</Button>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- 属性面板 - 使用TabControl -->
                    <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 属性面板标题 -->
                            <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="8,6">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⚙️" FontSize="12" VerticalAlignment="Center"/>
                                    <TextBlock Text="属性面板" Margin="6,0,0,0" FontWeight="Bold" FontSize="13" Foreground="#333333" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- 属性内容 - 使用TabControl -->
                            <TabControl Grid.Row="1" Background="#FFFFFF" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                                <!-- 工具参数标签页 -->
                                <TabItem Header="🔧 工具参数">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="4">
                                        <StackPanel>
                                            <!-- 节点信息 -->
                                            <Border Background="#E8F4F8" Padding="6" BorderBrush="#0066CC" BorderThickness="1" Margin="0,4,0,8" CornerRadius="3">
                                                <TextBlock Text="🔍 图像采集_1" FontWeight="Bold" FontSize="11" Foreground="#0066CC"/>
                                            </Border>

                                            <!-- 基本信息 -->
                                            <Expander Header="📋 基本信息" IsExpanded="True" Margin="0,4" Style="{StaticResource StyledExpander}">
                                                <StackPanel Margin="0,4">
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="名称:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <TextBox Grid.Column="1" Text="图像采集_1" Style="{StaticResource StyledTextBox}" FontSize="10"/>
                                                    </Grid>
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="ID:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <TextBlock Grid.Column="1" Text="node_001" VerticalAlignment="Center" FontSize="10" Foreground="#333333"/>
                                                    </Grid>
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="类型:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <TextBlock Grid.Column="1" Text="图像采集" VerticalAlignment="Center" FontSize="10" Foreground="#333333"/>
                                                    </Grid>
                                                </StackPanel>
                                                <Expander.HeaderTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="📋 基本信息" FontSize="10" FontWeight="Bold" Foreground="#333333"/>
                                                    </DataTemplate>
                                                </Expander.HeaderTemplate>
                                            </Expander>

                                            <!-- 参数配置 -->
                                            <Expander Header="🔧 参数配置" IsExpanded="True" Margin="0,4" Style="{StaticResource StyledExpander}">
                                                <StackPanel Margin="0,4">
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="相机:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <ComboBox Grid.Column="1" SelectedIndex="0" Style="{StaticResource StyledComboBox}" FontSize="10">
                                                            <ComboBoxItem Content="相机1"/>
                                                            <ComboBoxItem Content="相机2"/>
                                                        </ComboBox>
                                                    </Grid>
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="分辨率:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <ComboBox Grid.Column="1" SelectedIndex="0" Style="{StaticResource StyledComboBox}" FontSize="10">
                                                            <ComboBoxItem Content="1920x1080"/>
                                                            <ComboBoxItem Content="1280x720"/>
                                                            <ComboBoxItem Content="640x480"/>
                                                        </ComboBox>
                                                    </Grid>
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="40"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="曝光:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <TextBox Grid.Column="1" Text="10000" Style="{StaticResource StyledTextBox}" FontSize="10"/>
                                                        <TextBlock Grid.Column="2" Text="μs" VerticalAlignment="Center" FontSize="10" Foreground="#666666" Margin="4,0,0,0"/>
                                                    </Grid>
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="40"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="增益:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <TextBox Grid.Column="1" Text="10.5" Style="{StaticResource StyledTextBox}" FontSize="10"/>
                                                        <TextBlock Grid.Column="2" Text="dB" VerticalAlignment="Center" FontSize="10" Foreground="#666666" Margin="4,0,0,0"/>
                                                    </Grid>
                                                </StackPanel>
                                                <Expander.HeaderTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="🔧 参数配置" FontSize="10" FontWeight="Bold" Foreground="#333333"/>
                                                    </DataTemplate>
                                                </Expander.HeaderTemplate>
                                            </Expander>

                                            <!-- 性能统计 -->
                                            <Expander Header="📊 性能统计" IsExpanded="True" Margin="0,4" Style="{StaticResource StyledExpander}">
                                                <StackPanel Margin="0,4">
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="70"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="执行次数:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <TextBlock Grid.Column="1" Text="1,234" VerticalAlignment="Center" FontSize="10" FontWeight="Bold" Foreground="#0066CC"/>
                                                    </Grid>
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="70"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="平均时间:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <TextBlock Grid.Column="1" Text="8.5 ms" VerticalAlignment="Center" FontSize="10" FontWeight="Bold" Foreground="#0066CC"/>
                                                    </Grid>
                                                    <Grid Margin="0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="70"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="成功率:" VerticalAlignment="Center" FontSize="10" Foreground="#666666"/>
                                                        <TextBlock Grid.Column="1" Text="99.8%" VerticalAlignment="Center" FontSize="10" FontWeight="Bold" Foreground="#00CC99"/>
                                                    </Grid>
                                                </StackPanel>
                                                <Expander.HeaderTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="📊 性能统计" FontSize="10" FontWeight="Bold" Foreground="#333333"/>
                                                    </DataTemplate>
                                                </Expander.HeaderTemplate>
                                            </Expander>
                                        </StackPanel>
                                    </ScrollViewer>
                                </TabItem>

                                <!-- 信息标签页 -->
                                <TabItem Header="📋 信息">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="4">
                                        <TextBox IsReadOnly="True" Background="#F8F8F8" BorderThickness="0" FontFamily="{StaticResource MonoFont}" FontSize="10" Padding="8" TextWrapping="Wrap">
                                            <TextBox.Text>
                                                [14:23:45] ✅ [系统]
  系统启动成功
[14:23:46] ✅ [设备]
  相机1 连接成功
[14:23:46] ✅ [设备]
  相机2 连接成功
[14:23:47] ℹ️ [工作流]
  开始执行流程1
[14:23:48] ⚠️ [算法]
  图像采集超时
[14:23:49] ❌ [系统]
  执行失败
                                            </TextBox.Text>
                                        </TextBox>
                                    </ScrollViewer>
                                </TabItem>
                            </TabControl>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- ========== 状态栏 (24px) ========== -->
            <StatusBar Grid.ColumnSpan="3" Grid.Row="1" Height="24" Background="{StaticResource HeaderBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                <StatusBarItem>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="●" Foreground="#00CC99" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding StatusText}" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📷 相机:" FontSize="11" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding CameraStatus}" FontSize="11" Foreground="#00CC99" VerticalAlignment="Center"/>
                    </StackPanel>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <Border Background="#E6F2FF" BorderBrush="#0066CC" BorderThickness="1" CornerRadius="3" Padding="6,2" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="当前工作流:" FontSize="11" Foreground="#0066CC" VerticalAlignment="Center" Margin="0,0,4,0" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding CurrentWorkflow.Name, FallbackValue='无'}" FontSize="11" Foreground="#0066CC" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="Bold"/>
                            <TextBlock Text="状态:" FontSize="11" Foreground="#0066CC" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="{Binding CurrentWorkflow.IsRunning, Converter={StaticResource BoolToRunningTextConverter}, FallbackValue='已停止'}" FontSize="11" Foreground="#0066CC" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="CPU:" FontSize="11" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="45%" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="FPS:" FontSize="11" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="62.5" FontSize="11" FontWeight="Bold" Foreground="#00CC99" VerticalAlignment="Center"/>
                    </StackPanel>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </DockPanel>
</Window>
