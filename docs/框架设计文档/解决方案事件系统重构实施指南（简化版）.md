# 解决方案事件系统重构实施指南（简化版）

> **实施说明**：本指南提供完整的代码修改内容，可直接复制粘贴使用

## 📋 实施概览
- **目标**：重构事件系统，将粗粒度事件拆分为细粒度事件
- **原则**：不考虑向后兼容，完全删除旧代码
- **预期**：常见操作性能提升 10-1000 倍

## 🔧 阶段一：创建新文件

### 文件1：`src/Workflow/SolutionMetadataEventArgs.cs`（新建）

```csharp
namespace SunEyeVision.Workflow;

/// <summary>
/// 解决方案元数据事件参数
/// </summary>
public class SolutionMetadataEventArgs : EventArgs
{
    /// <summary>
    /// 元数据对象（完整）
    /// </summary>
    public SolutionMetadata? Metadata { get; set; }

    /// <summary>
    /// 解决方案ID
    /// </summary>
    public string? SolutionId { get; set; }

    /// <summary>
    /// 旧名称（用于重命名事件）
    /// </summary>
    public string? OldName { get; set; }

    /// <summary>
    /// 新名称（用于重命名事件）
    /// </summary>
    public string? NewName { get; set; }
}
```

---

## 🔧 阶段二：修改 SolutionManager.cs

### 修改1：删除旧事件（第86行）

**查找代码**：
```csharp
/// <summary>
/// 元数据变更事件
/// </summary>
public event EventHandler? MetadataChanged;
```

**替换为**：
```csharp
// ✅ 删除 MetadataChanged 事件（已被细粒度事件替代）
```

### 修改2：添加新事件定义（在第86行后添加）

**查找位置**：在第86行后、WorkflowAdded 事件前

**添加代码**：
```csharp
/// <summary>
/// 解决方案添加事件
/// </summary>
public event EventHandler<SolutionMetadataEventArgs>? SolutionAdded;

/// <summary>
/// 解决方案删除事件
/// </summary>
public event EventHandler<SolutionMetadataEventArgs>? SolutionRemoved;

/// <summary>
/// 解决方案重命名事件
/// </summary>
public event EventHandler<SolutionMetadataEventArgs>? SolutionRenamed;

/// <summary>
/// 解决方案更新事件
/// </summary>
public event EventHandler<SolutionMetadataEventArgs>? SolutionUpdated;

/// <summary>
/// 元数据刷新事件（全量）
/// </summary>
public event EventHandler? MetadataRefreshed;

/// <summary>
/// 当前解决方案变更事件
/// </summary>
public event EventHandler<SolutionMetadataEventArgs>? CurrentSolutionChanged;
```

### 修改3：OpenSolution 方法（第279行）

**查找代码**：
```csharp
// 触发事件
SolutionOpened?.Invoke(this, solution);
MetadataChanged?.Invoke(this, EventArgs.Empty);

_logger.Log(LogLevel.Success, $"打开解决方案: {metadata.Name} -> {filePath}", "SolutionManager");
```

**替换为**：
```csharp
// 触发事件
SolutionOpened?.Invoke(this, solution);

// ✅ 使用细粒度事件
var currentSolutionArgs = new SolutionMetadataEventArgs
{
    Metadata = metadata,
    SolutionId = metadata.Id
};
CurrentSolutionChanged?.Invoke(this, currentSolutionArgs);

_logger.Log(LogLevel.Success, $"打开解决方案: {metadata.Name} -> {filePath}", "SolutionManager");
```

### 修改4：SaveSolution 方法（第358行）

**查找代码**：
```csharp
// 触发事件
SolutionSaved?.Invoke(this, CurrentSolution);
MetadataChanged?.Invoke(this, EventArgs.Empty);

_logger.Log(LogLevel.Success, $"保存解决方案成功: Id={CurrentSolution.Id} -> {savePath}", "SolutionManager");
```

**替换为**：
```csharp
// 触发事件
SolutionSaved?.Invoke(this, CurrentSolution);

// ✅ 使用细粒度事件
var updatedArgs = new SolutionMetadataEventArgs
{
    Metadata = metadata,
    SolutionId = metadata.Id
};
SolutionUpdated?.Invoke(this, updatedArgs);

_logger.Log(LogLevel.Success, $"保存解决方案成功: Id={CurrentSolution.Id} -> {savePath}", "SolutionManager");
```

### 修改5：SaveAsSolution 方法（第415行）

**查找代码**：
```csharp
// 触发事件
SolutionSaved?.Invoke(this, CurrentSolution);
MetadataChanged?.Invoke(this, EventArgs.Empty);

_logger.Log(LogLevel.Success, $"另存为解决方案成功: Id={CurrentSolution.Id} -> {filePath}", "SolutionManager");
```

**替换为**：
```csharp
// 触发事件
SolutionSaved?.Invoke(this, CurrentSolution);

// ✅ 使用细粒度事件
var addedArgs = new SolutionMetadataEventArgs
{
    Metadata = metadata,
    SolutionId = metadata.Id
};
SolutionAdded?.Invoke(this, addedArgs);

var currentSolutionArgs = new SolutionMetadataEventArgs
{
    Metadata = metadata,
    SolutionId = metadata.Id
};
CurrentSolutionChanged?.Invoke(this, currentSolutionArgs);

_logger.Log(LogLevel.Success, $"另存为解决方案成功: Id={CurrentSolution.Id} -> {filePath}", "SolutionManager");
```

### 修改6：DeleteSolution 方法（第482行）

**查找代码**：
```csharp
SaveSettings();

// 触发事件
MetadataChanged?.Invoke(this, EventArgs.Empty);

_logger.Log(LogLevel.Success, $"删除解决方案: {metadata.Name} (Id={solutionId})", "SolutionManager");
```

**替换为**：
```csharp
SaveSettings();

// ✅ 使用细粒度事件
var removedArgs = new SolutionMetadataEventArgs
{
    SolutionId = solutionId
};
SolutionRemoved?.Invoke(this, removedArgs);

_logger.Log(LogLevel.Success, $"删除解决方案: {metadata.Name} (Id={solutionId})", "SolutionManager");
```

### 修改7：RenameSolutionFile 方法（第595行）

**查找代码**：
```csharp
// 10. 触发事件
MetadataChanged?.Invoke(this, EventArgs.Empty);

_logger.Log(LogLevel.Success, $"重命名解决方案成功: {oldName} -> {newName}", "SolutionManager");
```

**替换为**：
```csharp
// 10. 触发事件
var renamedArgs = new SolutionMetadataEventArgs
{
    Metadata = metadata,
    SolutionId = metadata.Id,
    OldName = oldName,
    NewName = newName
};
SolutionRenamed?.Invoke(this, renamedArgs);

_logger.Log(LogLevel.Success, $"重命名解决方案成功: {oldName} -> {newName}", "SolutionManager");
```

### 修改8：CopySolution 方法（第665行）

**查找代码**：
```csharp
SaveSettings();

// 触发事件
MetadataChanged?.Invoke(this, EventArgs.Empty);

_logger.Log(LogLevel.Success, $"复制解决方案成功: {sourcePath} -> {targetFilePath}", "SolutionManager");
```

**替换为**：
```csharp
SaveSettings();

// ✅ 使用细粒度事件
var addedArgs = new SolutionMetadataEventArgs
{
    Metadata = metadata,
    SolutionId = metadata.Id
};
SolutionAdded?.Invoke(this, addedArgs);

_logger.Log(LogLevel.Success, $"复制解决方案成功: {sourcePath} -> {targetFilePath}", "SolutionManager");
```

### 修改9：UpdateMetadata 方法（第711行）

**查找代码**：
```csharp
SaveSettings();

// 触发事件
MetadataChanged?.Invoke(this, EventArgs.Empty);

_logger.Log(LogLevel.Success, $"更新解决方案元数据: {metadata.Name} (Id={metadata.Id})", "SolutionManager");
```

**替换为**：
```csharp
SaveSettings();

// ✅ 使用细粒度事件
var updatedArgs = new SolutionMetadataEventArgs
{
    Metadata = metadata,
    SolutionId = metadata.Id
};
SolutionUpdated?.Invoke(this, updatedArgs);

_logger.Log(LogLevel.Success, $"更新解决方案元数据: {metadata.Name} (Id={metadata.Id})", "SolutionManager");
```

### 修改10：RefreshMetadata 方法（第869行）

**查找代码**：
```csharp
// ✅ 步骤9：触发事件通知 UI 层
MetadataChanged?.Invoke(this, EventArgs.Empty);
```

**替换为**：
```csharp
// ✅ 步骤9：触发事件通知 UI 层
MetadataRefreshed?.Invoke(this, EventArgs.Empty);
```

### 修改11：ScanDirectory 方法（第959行）

**查找代码**：
```csharp
_logger.Log(LogLevel.Success, $"扫描目录完成: 扫描到 {metadataList.Count} 个解决方案, 注册了 {registeredCount} 个", "SolutionManager");

// 触发事件
MetadataChanged?.Invoke(this, EventArgs.Empty);
```

**替换为**：
```csharp
_logger.Log(LogLevel.Success, $"扫描目录完成: 扫描到 {metadataList.Count} 个解决方案, 注册了 {registeredCount} 个", "SolutionManager");

// ✅ 触发事件
MetadataRefreshed?.Invoke(this, EventArgs.Empty);
```

---

## 🔧 阶段三：修改 SolutionConfigurationDialogViewModel.cs

### 修改1：删除旧事件订阅（第89行）

**查找代码**：
```csharp
// 监听元数据变更事件，自动刷新UI
_solutionManager.MetadataChanged += (sender, e) => LoadSolutions();
```

**替换为**：
```csharp
// ✅ 监听细粒度事件，实现增量更新
_solutionManager.SolutionAdded += OnSolutionAdded;
_solutionManager.SolutionRemoved += OnSolutionRemoved;
_solutionManager.SolutionRenamed += OnSolutionRenamed;
_solutionManager.SolutionUpdated += OnSolutionUpdated;
_solutionManager.MetadataRefreshed += OnMetadataRefreshed;
_solutionManager.CurrentSolutionChanged += OnCurrentSolutionChanged;
```

### 修改2：添加事件处理方法（在 LoadSolutions 方法后添加）

**查找位置**：在 LoadSolutions 方法后（第705行后）

**添加代码**：
```csharp
/// <summary>
/// 解决方案添加事件处理
/// </summary>
/// <remarks>
/// 增量更新：直接添加到集合，不重新加载
/// </remarks>
private void OnSolutionAdded(object? sender, SolutionMetadataEventArgs e)
{
    if (e.Metadata != null)
    {
        SolutionMetadatas.Add(e.Metadata);
        LogInfo($"添加解决方案到列表: {e.Metadata.Name}");
    }
}

/// <summary>
/// 解决方案删除事件处理
/// </summary>
/// <remarks>
/// 增量更新：直接从集合移除，不重新加载
/// </remarks>
private void OnSolutionRemoved(object? sender, SolutionMetadataEventArgs e)
{
    var toRemove = SolutionMetadatas.FirstOrDefault(s => s.Id == e.SolutionId);
    if (toRemove != null)
    {
        SolutionMetadatas.Remove(toRemove);
        LogInfo($"从列表移除解决方案: {toRemove.Name}");
    }
}

/// <summary>
/// 解决方案重命名事件处理
/// </summary>
/// <remarks>
/// 增量更新：直接更新属性，不重新加载
/// </remarks>
private void OnSolutionRenamed(object? sender, SolutionMetadataEventArgs e)
{
    var toUpdate = SolutionMetadatas.FirstOrDefault(s => s.Id == e.Metadata?.Id);
    if (toUpdate != null && e.Metadata != null)
    {
        toUpdate.Name = e.Metadata.Name;
        toUpdate.FilePath = e.Metadata.FilePath;
        toUpdate.DirectoryPath = e.Metadata.DirectoryPath;
        toUpdate.ModifiedTime = e.Metadata.ModifiedTime;
        LogInfo($"重命名解决方案: {e.OldName} → {e.NewName}");
    }
}

/// <summary>
/// 解决方案更新事件处理
/// </summary>
/// <remarks>
/// 增量更新：直接更新属性，不重新加载
/// </remarks>
private void OnSolutionUpdated(object? sender, SolutionMetadataEventArgs e)
{
    var toUpdate = SolutionMetadatas.FirstOrDefault(s => s.Id == e.Metadata?.Id);
    if (toUpdate != null && e.Metadata != null)
    {
        toUpdate.Name = e.Metadata.Name;
        toUpdate.Description = e.Metadata.Description;
        toUpdate.ModifiedTime = e.Metadata.ModifiedTime;
        LogInfo($"更新解决方案元数据: {e.Metadata.Name}");
    }
}

/// <summary>
/// 元数据刷新事件处理
/// </summary>
/// <remarks>
/// 全量刷新：重新加载整个列表
/// 触发场景：启动扫描、手动扫描、手动刷新
/// </remarks>
private void OnMetadataRefreshed(object? sender, EventArgs e)
{
    LoadSolutions();
    LogInfo("元数据已刷新，重新加载列表");
}

/// <summary>
/// 当前解决方案变更事件处理
/// </summary>
/// <remarks>
/// 增量更新：更新 CurrentSolutionId，触发属性通知
/// </remarks>
private void OnCurrentSolutionChanged(object? sender, SolutionMetadataEventArgs e)
{
    OnPropertyChanged(nameof(CurrentSolutionId));
    LogInfo($"当前解决方案变更: {e.Metadata?.Name}");
}
```

---

## 📊 实施检查清单

### 阶段一检查
- [ ] 创建 `src/Workflow/SolutionMetadataEventArgs.cs` 文件
- [ ] 文件内容正确（4个属性）

### 阶段二检查（SolutionManager.cs）
- [ ] 删除第86行 `MetadataChanged` 事件
- [ ] 添加6个新事件定义
- [ ] 修改 OpenSolution 方法（第279行）
- [ ] 修改 SaveSolution 方法（第358行）
- [ ] 修改 SaveAsSolution 方法（第415行）
- [ ] 修改 DeleteSolution 方法（第482行）
- [ ] 修改 RenameSolutionFile 方法（第595行）
- [ ] 修改 CopySolution 方法（第665行）
- [ ] 修改 UpdateMetadata 方法（第711行）
- [ ] 修改 RefreshMetadata 方法（第869行）
- [ ] 修改 ScanDirectory 方法（第959行）

### 阶段三检查（SolutionConfigurationDialogViewModel.cs）
- [ ] 删除第89行旧事件订阅
- [ ] 添加6个新事件订阅
- [ ] 添加 OnSolutionAdded 方法
- [ ] 添加 OnSolutionRemoved 方法
- [ ] 添加 OnSolutionRenamed 方法
- [ ] 添加 OnSolutionUpdated 方法
- [ ] 添加 OnMetadataRefreshed 方法
- [ ] 添加 OnCurrentSolutionChanged 方法

### 编译检查
- [ ] 项目编译无错误
- [ ] Linter 检查通过
- [ ] 无警告或仅有已知警告

### 功能检查
- [ ] 新建解决方案功能正常
- [ ] 打开解决方案功能正常
- [ ] 保存解决方案功能正常
- [ ] 删除解决方案功能正常
- [ ] 重命名解决方案功能正常
- [ ] 复制解决方案功能正常
- [ ] 编辑解决方案功能正常
- [ ] 启动解决方案功能正常

---

## ⚠️ 常见问题

### Q1: 编译错误 "找不到 SolutionMetadataEventArgs"
**A**: 确保先创建了 `SolutionMetadataEventArgs.cs` 文件

### Q2: 事件订阅时参数不匹配
**A**: 确保事件订阅代码与事件定义的签名一致

### Q3: 事件处理方法中的 Lambda 表达式报错
**A**: 确保正确实现事件处理方法，不要使用 Lambda 表达式

### Q4: 日志过多
**A**: 检查是否有其他地方订阅了旧事件，确保所有使用点都已更新

---

## 🚀 实施后验证

### 性能验证
1. 添加 100 个解决方案，观察响应时间
2. 重命名 100 个解决方案，观察响应时间
3. 对比实施前后的性能差异

### 功能验证
1. 测试所有解决方案管理功能
2. 验证事件是否正确触发
3. 验证 UI 是否正确更新

### 日志验证
1. 检查日志输出是否正常
2. 确认没有使用 Debug.WriteLine
3. 确认日志级别使用恰当

---

**实施原则**：rule-007 原型设计期代码纯净原则
**目标**：性能提升 10-1000 倍
**质量**：符合所有项目规范
