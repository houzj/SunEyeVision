<Window x:Class="SunEyeVision.PluginSystem.Tools.ImageSaveTool.UI.ImageSaveToolDebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SunEyeVision.PluginSystem.Tools.ImageSaveTool.ViewModels"
        mc:Ignorable="d"
        Title="图像保存 - 调试窗口" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SunEyeVision.UI;component/Resources/AppResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!-- 主内容区 -->
    <Border Grid.Row="1" Margin="8" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">

                <!-- 基本参数 -->
                <Border Background="#F0F8FF" Padding="12,8" BorderBrush="#0066CC" BorderThickness="0,0,0,2" Margin="0,0,0,12">
                    <TextBlock Text="📋 基本参数" FontWeight="Bold" FontSize="14" Foreground="#0066CC"/>
                </Border>

                <!-- 文件路径 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Column="0" Grid.Row="0" Text="文件路径:" VerticalAlignment="Center" FontSize="12" Foreground="#333333" Margin="0,0,8,0"/>
                    <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"
                             Padding="6" FontSize="12" VerticalContentAlignment="Center"/>
                    <Button Grid.Column="2" Grid.Row="0" Content="浏览..." Padding="8,4"
                            Command="{Binding BrowseFilePathCommand}" Margin="8,0,0,0"/>

                    <TextBlock Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1"
                               Text="图像保存的完整路径，支持PNG、JPEG、BMP、TIFF格式"
                               FontSize="10" Foreground="#999999" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </Grid>

                <!-- 图像格式 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Column="0" Grid.Row="0" Text="图像格式:" VerticalAlignment="Center" FontSize="12" Foreground="#333333" Margin="0,0,8,0"/>
                    <ComboBox Grid.Column="1" Grid.Row="0" ItemsSource="{Binding ImageFormats}"
                              SelectedItem="{Binding ImageFormat}" Padding="6" FontSize="12" VerticalContentAlignment="Center"/>

                    <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                               Text="保存的图像格式，不同格式适用于不同场景"
                               FontSize="10" Foreground="#999999" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </Grid>

                <!-- 图像质量 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Column="0" Grid.Row="0" Text="图像质量:" VerticalAlignment="Center" FontSize="12" Foreground="#333333" Margin="0,0,8,0"/>
                    <Slider Grid.Column="1" Grid.Row="0" Value="{Binding ImageQuality}" Minimum="1" Maximum="100"
                           VerticalAlignment="Center" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Grid.Column="2" Grid.Row="0" Text="{Binding ImageQuality}" VerticalAlignment="Center"
                               FontSize="12" FontWeight="Bold" Foreground="#0066CC" Margin="8,0,0,0"/>

                    <TextBlock Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1"
                               Text="JPEG格式的压缩质量(1-100)，数值越高图像质量越好但文件越大"
                               FontSize="10" Foreground="#999999" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </Grid>

                <!-- 覆盖已有文件 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Column="0" Grid.Row="0" Text="覆盖已有文件:" VerticalAlignment="Center" FontSize="12" Foreground="#333333" Margin="0,0,8,0"/>
                    <CheckBox Grid.Column="1" Grid.Row="0" IsChecked="{Binding OverwriteExisting}"
                             VerticalAlignment="Center" Content="如果目标文件已存在，是否覆盖" FontSize="12"/>

                    <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                               Text="不勾选则会在文件名后添加时间戳以避免冲突"
                               FontSize="10" Foreground="#999999" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </Grid>

                <!-- 高级参数标题 -->
                <Border Background="#FFF8F0" Padding="12,8" BorderBrush="#FF9900" BorderThickness="0,0,0,2" Margin="16,0,0,12">
                    <TextBlock Text="⚙️ 高级设置" FontWeight="Bold" FontSize="14" Foreground="#FF9900"/>
                </Border>

                <!-- 执行控制 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                    <Button Content="保存图像" Padding="20,10" FontSize="13" FontWeight="Bold"
                           Background="#00CC99" Foreground="White" BorderBrush="#00AA77" BorderThickness="1"
                           Cursor="Hand" Margin="0,0,8,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="重置参数" Padding="20,10" FontSize="13"
                           Background="#F0F0F0" Foreground="#666666" BorderBrush="#CCCCCC" BorderThickness="1"
                           Cursor="Hand" Margin="8,0,0,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>
