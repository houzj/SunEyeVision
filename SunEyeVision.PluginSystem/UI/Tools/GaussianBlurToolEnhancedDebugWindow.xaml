<local:EnhancedToolDebugWindow x:Class="SunEyeVision.PluginSystem.UI.Tools.GaussianBlurToolEnhancedDebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SunEyeVision.PluginSystem.UI"
        xmlns:vm="clr-namespace:SunEyeVision.PluginSystem.Tools.GaussianBlurTool.ViewModels"
        mc:Ignorable="d"
        Title="高斯模糊工具 - 调试窗口" Height="700" Width="900">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SunEyeVision.UI;component/Resources/AppResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <!-- 使用基类提供的完整布局 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#2196F3" Padding="16,12" BorderBrush="#1976D2" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔧" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding ToolName}" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text=" - 调试模式" FontSize="14" Foreground="#BBDEFB" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 错误提示区 -->
        <Border Grid.Row="1" Background="#FFEBEE" Padding="12,8" BorderBrush="#FFCDD2" BorderThickness="0,0,0,1"
                Visibility="{Binding ValidationError, Converter={StaticResource StringToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠️" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding ValidationError}" FontSize="13" Foreground="#D32F2F" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 参数配置卡片 -->
                <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="⚙️" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="参数配置" FontSize="16" FontWeight="SemiBold" Foreground="#333333" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- 动态参数控件 -->
                        <ItemsControl ItemsSource="{Binding ParameterItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type local:ParameterItem}">
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="180"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 参数标签 -->
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="13" Foreground="#333333"/>
                                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#666666" TextWrapping="Wrap" Margin="0,2,0,0"
                                                       Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        </StackPanel>

                                        <!-- 参数控件 -->
                                        <ContentPresenter Grid.Column="1" Content="{Binding Control}" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- 预览卡片 -->
                <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="🖼️" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="预览" FontSize="16" FontWeight="SemiBold" Foreground="#333333" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Grid Height="300" Background="#F5F5F5">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="📷" FontSize="48" Foreground="#CCCCCC" HorizontalAlignment="Center"/>
                                <TextBlock Text="图像预览区域" FontSize="14" Foreground="#999999" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 执行进度卡片 -->
                <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,12"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="⏳" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="执行进度" FontSize="16" FontWeight="SemiBold" Foreground="#333333" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ProgressBar Grid.Row="0" Height="8" Value="{Binding Progress}" Maximum="100"
                                         Background="#E0E0E0" Foreground="#2196F3" BorderThickness="0"/>

                            <Grid Grid.Row="1" Margin="0,8,0,0">
                                <TextBlock Text="{Binding Progress, Converter={StaticResource ProgressToStringConverter}}" FontSize="12" Foreground="#666666"/>
                                <TextBlock Text="{Binding ProgressMessage}" FontSize="12" Foreground="#666666" HorizontalAlignment="Right"/>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 执行结果卡片 -->
                <Border Grid.Row="3" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="📊" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="执行结果" FontSize="16" FontWeight="SemiBold" Foreground="#333333" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#F5F5F5" CornerRadius="4" Padding="12" Margin="0,0,6,0">
                                <StackPanel>
                                    <TextBlock Text="核大小" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding KernelSize}" FontSize="16" FontWeight="Bold" Foreground="#333333"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Background="#F5F5F5" CornerRadius="4" Padding="12" Margin="6,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="标准差" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding Sigma, StringFormat={}{0:F2}}" FontSize="16" FontWeight="Bold" Foreground="#333333"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- 操作按钮区 -->
        <Border Grid.Row="3" Background="White" Padding="16,12" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Background="#2196F3" Foreground="White" BorderBrush="#1976D2" BorderThickness="1" Padding="20,10"
                        Command="{Binding RunCommand}" Margin="4,0" FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="▶" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="运行" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Button Background="#FF5252" Foreground="White" BorderBrush="#D32F2F" BorderThickness="1" Padding="20,10"
                        Command="{Binding CancelCommand}" Margin="4,0" FontWeight="SemiBold" Cursor="Hand"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏹" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="取消" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Button Background="White" Foreground="#333333" BorderBrush="#CCCCCC" BorderThickness="1" Padding="20,10"
                        Command="{Binding ValidateCommand}" Margin="4,0" FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✓" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="验证参数" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Button Background="White" Foreground="#333333" BorderBrush="#CCCCCC" BorderThickness="1" Padding="20,10"
                        Command="{Binding ResetCommand}" Margin="4,0" FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="↺" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="重置" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Separator Width="1" Margin="16,4"/>

                <Button Background="White" Foreground="#333333" BorderBrush="#CCCCCC" BorderThickness="1" Padding="20,10"
                        Command="{Binding SaveCommand}" Margin="4,0" FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="保存" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Button Background="White" Foreground="#333333" BorderBrush="#CCCCCC" BorderThickness="1" Padding="20,10"
                        Command="{Binding LoadCommand}" Margin="4,0" FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📂" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="加载" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Separator Width="1" Margin="16,4"/>

                <Button Background="White" Foreground="#333333" BorderBrush="#CCCCCC" BorderThickness="1" Padding="20,10"
                        Command="{Binding CreateSnapshotCommand}" Margin="4,0" FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📷" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="快照" VerticalAlignment="Center" FontSize="14"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- 底部状态栏 -->
        <Border Grid.Row="4" Background="#FAFAFA" Padding="12,8" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" FontSize="12" Foreground="#333333" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                        <TextBlock FontSize="11" Foreground="#1565C0">
                            <Run Text="执行时间: "/>
                            <Run Text="{Binding ExecutionTime}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </Border>

                    <Border Background="#E8F5E9" CornerRadius="4" Padding="8,4">
                        <TextBlock FontSize="11" Foreground="#2E7D32">
                            <Run Text="FPS: "/>
                            <Run Text="{Binding FPS}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</local:EnhancedToolDebugWindow>
