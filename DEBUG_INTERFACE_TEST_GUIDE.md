# 调试界面测试指南

## 概述
本文档描述了如何测试 SunEyeVision 的混合模式调试界面。调试界面支持两种模式：
1. **自动生成控件** - 基于参数元数据自动生成UI控件
2. **自定义控件** - 工具提供完全自定义的调试界面

## 已注册的工具插件

### 1. 图像采集 (ImageCapture) 📷
- **工具ID**: `image_capture`
- **分类**: 采集
- **参数类型**:
  - `cameraId` (Int) - 相机设备ID [0-10]
  - `timeout` (Int) - 采集超时时间 [100-60000 ms]
  - `triggerMode` (Enum) - 触发模式
- **测试重点**: Int类型控件、Enum下拉框

### 2. 高斯模糊 (GaussianBlur) 🌫️
- **工具ID**: `gaussian_blur`
- **分类**: 图像处理
- **参数类型**:
  - `kernelSize` (Int) - 高斯核大小 [3-99, 奇数]
  - `sigma` (Double) - 标准差 [0.1-10.0]
  - `borderType` (Enum) - 边界类型
- **测试重点**: Int+Slider组合、Double+Slider组合、数值范围验证

### 3. 模板匹配定位 (TemplateMatching) 🎯
- **工具ID**: `template_matching`
- **分类**: 定位
- **参数类型**:
  - `templateImage` (Image) - 模板图像
  - `threshold` (Double) - 匹配阈值 [0.0-1.0]
  - `method` (Enum) - 匹配方法
  - `minSize` (Int) - 最小尺寸
- **测试重点**: Image文件选择控件、Double精度控制

### 4. OCR识别 (OCR) 📝
- **工具ID**: `ocr_recognition`
- **分类**: 识别
- **参数类型**:
  - `language` (Enum) - 识别语言
  - `confThreshold` (Double) - 置信度阈值 [0.0-100.0]
  - `whitelist` (String) - 白名单
  - `enableDenoise` (Bool) - 启用降噪
- **测试重点**: String控件（单行/多行）、CheckBox布尔控件

### 5. 图像阈值化 (Threshold) 🎚️
- **工具ID**: `threshold`
- **分类**: 图像处理
- **参数类型**:
  - `threshold` (Int) - 阈值 [0-255]
  - `maxValue` (Int) - 最大值 [0-255]
  - `type` (Enum) - 阈值类型
  - `adaptiveMethod` (Enum) - 自适应方法
  - `blockSize` (Int) - 块大小 [3-31, 奇数]
  - `invert` (Bool) - 反转结果
- **测试重点**: 多个Int参数、组合验证逻辑

### 6. 边缘检测 (EdgeDetection) 📐
- **工具ID**: `edge_detection`
- **分类**: 图像处理
- **参数类型**:
  - `method` (Enum) - 检测方法
  - `threshold1` (Double) - 低阈值 [0.0-255.0]
  - `threshold2` (Double) - 高阈值 [0.0-255.0]
  - `apertureSize` (Int) - 孔径大小 [1-7]
  - `L2gradient` (Bool) - L2梯度
  - `kernelSize` (Int) - 卷积核大小 [1-5]
- **测试重点**: 跨参数依赖验证（threshold1 < threshold2）

### 7. ROI裁剪 (ROICrop) ✂️
- **工具ID**: `roi_crop`
- **分类**: 图像处理
- **参数类型**:
  - `roi` (Rect) - ROI区域
  - `padding` (Int) - 边距填充
  - `clipToImage` (Bool) - 限制在图像内
  - `outputWidth` (Int) - 输出宽度
  - `outputHeight` (Int) - 输出高度
- **测试重点**: Rect参数（X, Y, W, H四个文本框）

### 8. 颜色空间转换 (ColorConvert) 🎨
- **工具ID**: `color_convert`
- **分类**: 图像处理
- **参数类型**:
  - `targetColorSpace` (Enum) - 目标颜色空间
  - `sourceColorSpace` (Enum) - 源颜色空间
  - `channels` (Int) - 输出通道数
- **测试重点**: 多个Enum参数

## 测试步骤

### 1. 启动应用程序
```bash
cd d:/MyWork/SunEyeVision/SunEyeVision/SunEyeVision.UI/bin/Debug/net9.0
SunEyeVision.UI.exe
```

### 2. 测试自动生成控件
1. 从工具箱拖拽任意工具到画布
2. **双击节点**打开调试窗口
3. 检查以下内容：
   - ✅ 参数按分类分组显示
   - ✅ 必选参数带 `*` 标记
   - ✅ 参数描述正确显示
   - ✅ 数值范围正确
   - ✅ 默认值正确
   - ✅ Enum参数显示下拉框
   - ✅ Bool参数显示CheckBox
   - ✅ Image/FilePath参数显示浏览按钮

### 3. 测试参数类型控件

#### Int + Slider 组合
- 选择 **高斯模糊** 工具
- 调整 `kernelSize` 参数
- 验证：Slider和TextBox同步

#### Double + Slider 组合
- 选择 **高斯模糊** 工具
- 调整 `sigma` 参数
- 验证：支持小数值

#### Enum 下拉框
- 选择 **OCR识别** 工具
- 更改 `language` 参数
- 验证：显示所有选项（CN, EN, JP, KR, Mixed）

#### CheckBox 布尔控件
- 选择 **OCR识别** 工具
- 切换 `enableDenoise` 参数
- 验证：正确勾选/取消

#### Point 参数
- 选择 **模板匹配定位** 工具
- 查看输出参数 `position`
- 验证：显示 X 和 Y 文本框

#### Rect 参数
- 选择 **ROI裁剪** 工具
- 查看 `roi` 参数
- 验证：显示 X, Y, W, H 四个文本框

### 4. 测试参数验证
1. 打开 **图像阈值化** 工具
2. 将 `threshold` 设置为 -1（超出范围）
3. 观察是否有验证错误提示
4. 将 `blockSize` 设置为 4（偶数）
5. 观察是否有验证错误

### 5. 测试参数提取
1. 打开任意工具的调试窗口
2. 修改参数值
3. 点击"运行"按钮
4. 检查参数是否正确传递

## 参数类型支持矩阵

| 参数类型 | 控件类型 | 示例工具 |
|---------|---------|-----------|
| Int | TextBox + Slider (有范围时) | 高斯模糊 |
| Double | TextBox + Slider (有范围时) | 高斯模糊 |
| Bool | CheckBox | OCR识别 |
| Enum | ComboBox | OCR识别 |
| String | TextBox (单行/多行) | OCR识别 |
| Image | TextBox + 浏览按钮 | 模板匹配 |
| FilePath | TextBox + 浏览按钮 | (待测试) |
| Point | X + Y TextBox组合 | 模板匹配输出 |
| Size | 宽 + 高 TextBox组合 | (待测试) |
| Rect | X + Y + W + H TextBox组合 | ROI裁剪 |
| Color | 按钮 + TextBox | (待测试) |
| List | ListBox | (待测试) |

## 已知问题和注意事项

1. **自定义控件示例**: `DebugControlExample` 演示了如何实现完全自定义的调试界面
2. **参数分组**: 参数通过 `Category` 字段分组显示
3. **必需参数**: 带 `*` 标记的参数为必填项
4. **可编辑标记**: `EditableInDebug = true` 的参数在调试模式下可实时编辑

## 下一步

1. ✅ 所有8个工具插件已添加完整元数据
2. ✅ 支持13种参数类型的UI控件
3. ⚠️ 待完善：自定义控件的参数值提取逻辑
4. ⚠️ 待完善：参数验证反馈机制
5. ⚠️ 待完善：图像显示功能

## 构建和运行

```bash
# 构建解决方案
dotnet build SunEyeVision.sln

# 运行应用程序
cd SunEyeVision.UI/bin/Debug/net9.0
SunEyeVision.UI.exe
```

## 联系方式

如有问题，请参考以下文件：
- `SunEyeVision.PluginSystem/ToolMetadata.cs` - 元数据定义
- `SunEyeVision.PluginSystem/ParameterMetadata.cs` - 参数元数据定义
- `SunEyeVision.UI/ParameterControlFactory.cs` - UI控件工厂
- `SunEyeVision.UI/DebugWindow.xaml.cs` - 调试窗口实现
